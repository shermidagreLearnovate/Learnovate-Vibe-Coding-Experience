Task 4.1
The Goal: Add a feature where clicking a task title turns it into an input field to rename it.
Evaluation: "Surgical Precision." This tests if the model can modify 5% of a file without rewriting (and potentially breaking) the other 95%.

Sample Prompt: "Modify the TaskCard component. When I click on the task title, it should turn into an input field so I can rename it. Pressing 'Enter' or clicking away should save the new title."