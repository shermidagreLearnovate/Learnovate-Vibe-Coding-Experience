Task 3.3
The Goal: Replace the frontend mock data with an useEffect or react-query fetch to the NestJS API.
Evaluation: "The Connection Gap." Does it handle CORS issues? Does it remember that the backend is running on port 3000 but the frontend is on 5173?

Sample Prompt: "In the React app, replace the mock data with a real API call to the NestJS backend to fetch and display the tasks on the Kanban board. Handle CORS if necessary."