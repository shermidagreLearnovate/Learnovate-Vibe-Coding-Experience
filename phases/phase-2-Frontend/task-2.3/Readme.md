Task 2.3
The Goal: Create a types.ts file and a mockData.ts to populate the UI.
Evaluation: "Syntactic Foresight." Are the interfaces it generates compatible with what it wrote in the Prisma schema earlier?

Sample Prompt: "Generate TypeScript interfaces for my Tasks and Projects based on the Prisma schema. Then, create a mockData file with sample tasks so I can see the Kanban board in action."