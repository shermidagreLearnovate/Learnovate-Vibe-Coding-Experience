{
  "name": "task-management-monorepo",
  "private": true,
  "description": "Vibe-coding research project - Task Management System",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "frontend:dev": "npm run dev -w frontend",
    "backend:dev": "npm run start:dev -w backend",
    "install:all": "npm install && npm install -w frontend && npm install -w backend"
  }
}
