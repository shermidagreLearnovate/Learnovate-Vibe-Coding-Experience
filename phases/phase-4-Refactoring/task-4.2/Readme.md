Task 4.2
The Goal: Add a "Loading" spinner and a "Toast" notification if the API call fails.
Evaluation: "User Empathy." Does the model suggest a good UX library (like Lucide icons or Sonner toasts), or does it do the bare minimum?

Sample Prompt: "Add a loading spinner that shows while tasks are being fetched. Also, integrate a toast notification system to show a success message when a task is renamed or moved."