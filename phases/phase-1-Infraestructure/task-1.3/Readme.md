Task 1.3
The Goal: Create the Dockerfile and docker-compose.yaml.
Evaluation: "The Glue Factor." This is usually where "vibes" fail. How much manual tweaking is needed to get the NestJS container to wait for the Postgres container to be healthy?

Sample Prompt: "Create Dockerfiles for the frontend and backend, and a docker-compose.yaml file that sets up both apps along with a PostgreSQL database. Make sure the backend can connect to the DB on startup."